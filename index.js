function superbowlWin (collection){
for (const item of collection) {
    if (item.result==="W") {
        return item.year
        {
            return undefined
        }
    }
    }
}
record.find(superbowlWin)